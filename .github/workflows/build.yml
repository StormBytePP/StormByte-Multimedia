name: Compile & Test

permissions:
  actions: write

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
  
  workflow_dispatch:

env:
  BUILD_TYPE: Debug

jobs:
  build-linux:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        compiler: [gcc, clang, clang-libc++]
        ffmpeg: [SYSTEM, BUNDLED]
        nonfree: [ON, OFF]
        gpl: [ON, OFF]
        exclude:
          - compiler: clang-libc++
            ffmpeg: SYSTEM
          - ffmpeg: SYSTEM
            nonfree: ON
          - ffmpeg: SYSTEM
            gpl: ON
    
    name: ${{ matrix.compiler }}-ffmpeg(${{ matrix.ffmpeg }})-nonfree-(${{ matrix.nonfree }})-gpl(${{ matrix.gpl }})

    steps:
    - name: Checkout repository and submodules
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        submodules: recursive

    - name: Install toolchain
      id: install-toolchain
      uses: stormbytepp/githubactions/.github/actions/install-toolchain@master
      with:
        toolchain: ${{ matrix.compiler }}
        ccache-key: '${{ matrix.compiler }}-ffmpeg(${{ matrix.ffmpeg }})-nonfree-(${{ matrix.nonfree }})-gpl(${{ matrix.gpl }})-v1'
      env:
        GITHUB_TOKEN: ${{ github.token }}

    - name: Install APT packages
      if: ${{ matrix.ffmpeg == 'SYSTEM' }}
      uses: stormbytepp/githubactions/.github/actions/install-cached@master
      with:
        cache-key: apt-ffmpeg-system-deps
        packages: 'ffmpeg libavcodec-dev libavformat-dev libavutil-dev libavfilter-dev libswscale-dev libswresample-dev'

    - name: Install APT packages
      if: ${{ matrix.ffmpeg == 'BUNDLED' }}
      uses: stormbytepp/githubactions/.github/actions/install-cached@master
      with:
        cache-key: apt-nasm-yasm
        packages: 'nasm yasm'

    - name: Add FFmpeg libs to LD_LIBRARY_PATH
      if: ${{ matrix.ffmpeg == 'BUNDLED' }}
      run: |
        echo "LD_LIBRARY_PATH=${{ github.workspace }}/build/thirdparty/ffmpeg/install/lib" >> $GITHUB_ENV

    - name: Build with CMake
      uses: ashutoshvarma/action-cmake-build@master
      with:
        build-dir: ${{ github.workspace }}/build
        cc: ${{ steps.install-toolchain.outputs.c_compiler }}
        cxx: ${{ steps.install-toolchain.outputs.cxx_compiler }}
        configure-options: >-
          -G Ninja
          -DENABLE_ASAN=ON
          -DENABLE_TEST=ON
          -DWITH_FFMPEG=${{ matrix.ffmpeg }}
          -DWITH_NONFREE=${{ matrix.nonfree }}
          -DWITH_GPL=${{ matrix.gpl }}
          -DWITH_STORMBYTE=BUNDLED
          ${{ matrix.compiler == 'clang-libc++' && format('-DCMAKE_CXX_FLAGS=-stdlib={0}', 'libc++') || '' }}
        build-type: ${{ env.BUILD_TYPE }}
        run-test: true
        ctest-options: --output-on-failure --test-dir ${{ github.workspace }}/build/test

  build-windows:
    runs-on: windows-latest
    strategy:
      matrix:
        arch:
          - x64

    steps:
      - name: Checkout repository and submodules
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Install toolchain
        id: install-toolchain
        uses: stormbytepp/githubactions/.github/actions/install-toolchain@master
        with:
          toolchain: msvc

      - name: Install Nasm and Yasm
        shell: pwsh
        run: |
          choco install nasm yasm -y

      - name: Build with CMake
        uses: ashutoshvarma/action-cmake-build@master
        with:
          build-dir: ${{ github.workspace }}\build
          configure-options: >-
            -G Ninja
            -DENABLE_TEST=ON
            -DWITH_FFMPEG=BUNDLED
            -DWITH_STORMBYTE=BUNDLED
            -DWITH_NONFREE=ON
            -DWITH_GPL=ON
          build-type: ${{ env.BUILD_TYPE }}

      - name: Add DLL directory to PATH
        run: |
          echo "${{ github.workspace }}\build\lib" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
          echo "${{ github.workspace }}\build\thirdparty\StormByte\base\lib" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
          echo "${{ github.workspace }}\build\thirdparty\ffmpeg\install\bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
          
      - name: Run unit tests
        run: ctest --output-on-failure --test-dir ${{ github.workspace }}\build\test
